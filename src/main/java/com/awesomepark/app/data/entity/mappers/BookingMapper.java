/*
package com.awesomepark.app.data.entity.mappers;

import com.awesomepark.app.data.entity.Booking;
import com.awesomepark.app.dto.BookingRequestDto;
import com.awesomepark.app.dto.BookingResponseDto;
import org.mapstruct.Mapper;
import org.mapstruct.factory.Mappers;
import org.springframework.stereotype.Component;

@Mapper(componentModel = "jsr330")
@Component
public interface BookingMapper {
    BookingMapper INSTANCE = Mappers.getMapper(BookingMapper.class);

    BookingRequestDto bookingToBookingRequestDto(Booking booking);
    BookingResponseDto bookingToBookingResponseDto(Booking booking);

    Booking bookingRequestDtoToBooking(BookingRequestDto dto);
    Booking bookingResponseDtoToBooking(BookingResponseDto dto);
}

*/
